function identity(arg) {
    return arg;
}

// Example usage
console.log(identity(5));           // Outputs: 5
console.log(identity("hello"));     // Outputs: hello
console.log(identity([1, 2, 3]));   // Outputs: [1, 2, 3]
